import { SettingsPage } from "@/components/listener/SettingsPage";

export const metadata = {
  title: "Listener Settings — FACE",
};

export default function ListenerSettingsPage() {
  return <SettingsPage />;
}
